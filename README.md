# גלים 90929 — אתר הכנה למבחן

**🌐 האתר באוויר: https://roisadeh5.github.io/galim-prep/**

שיתוף = שליחת הקישור. בלי הרשמה ובלי התקנה; ההתקדמות של כל אחד
נשמרת רק אצלו בדפדפן.

אתר לימוד סטטי בעברית (HTML+JS בלבד — בלי שרת, בלי חשבון, בלי התקנה).
נבנה מ-30 שאלוני עבר 2020–2026 והפתרונות הרשמיים, לפי `LEARNING-SCRIPT.md`
שבתיקיית `How to Build`.

## הרצה מקומית

```bash
cd ~/Desktop/Projects/galim-prep && python3 -m http.server 8000
```

ואז פתח http://localhost:8000

## מה יש באתר

| חלק | מצב |
|---|---|
| **8 עמודי נושא** (d01–d08) עם נגני אנימציה | ✅ |
| **מרכז תרגול** — מאמן אדפטיבי, מחברת טעויות, חזרה מרווחת | ✅ |
| **שאלות אמריקאיות** — בוחרים תשובה והאתר בודק מיד | ✅ 92 |
| **סימולטור מבחן** — 4 משבצות, 4 שעות, שעון אמיתי | ✅ |
| **ארכיון מבחנים** — 21 מבחנים, 84 שאלות, כולם עם פתרון | ✅ |
| **מעבדות חופשיות** — 5 מעבדות אינטראקטיביות עם פרמטרים חיים | ✅ |
| **פתרון מונפש לכל שאלת מבחן במאגר** — אחרי "בדוק" | ✅ 40/40 |
| **דף נוסחאות** — 6 עמודי A4 + PDF מוכן להדפסה | ✅ |
| **פתרונות מונפשים** — לפי שנה | ראה למטה |

### מאגר השאלות
**216 שאלות** — 40 שאלות מבחן מלאות · 84 שאלות ארכיון ·
**92 אמריקאיות עם בחירת תשובה ובדיקה מיידית** (מיקום התשובה הנכונה מאוזן).
בלי כפילויות מזהה.

### כיסוי הפתרונות המונפשים

**28 שאלות · 330 שלבים · 308 ויזואלים — כל שבעת השאלונים במלואם.**

| שאלון | מצב | שלבים |
|---|---|---|
| 2026 שאלון X | ✅ 4/4 | 31 |
| 2025 שאלון X | ✅ 4/4 | 49 |
| 2025 שאלון Y | ✅ 4/4 | 50 |
| 2024 שאלון X | ✅ 4/4 | 50 |
| 2024 שאלון Y | ✅ 4/4 | 53 |
| 2023 שאלון X | ✅ 4/4 | 48 |
| 2023 שאלון Y | ✅ 4/4 | 49 |

> **21 שגיאות נמצאו בפתרונות הרשמיים** תוך כדי הכתיבה — כל חישוב אומת
> מחדש ולא הועתק. בין היתר: שגיאת פי-36 בהספק, נוסחאות חסרות גורם 1/v,
> שגיאות ממדים, יחידות שגויות (W במקום J) — ושאלה אחת (2022 Y ש4)
> שהנתונים שלה סותרים את עצמם. הפירוט המלא ב-`STATUS.md`.


> 📋 **מצב מלא ומעודכן של הפרויקט — `STATUS.md`.** מה קיים, מה נשאר,
> ואילו שגיאות נמצאו בפתרונות הרשמיים.

## מבנה המבחן

4 שאלות × 25 נק', 4 שעות, 6 עמודי נוסחאות מותרים.
פירוט מלא של המשבצות, הווריאציות והמלכודות החוזרות — ב-`EXAM-MAP.md`.

## מפת הקבצים

**תוכן — כאן מוסיפים:**

| קובץ | תפקיד |
|---|---|
| `config.js` | כל מה שתלוי בקורס — הקובץ היחיד שמגדיר את הקורס |
| `index.html` | עמודי הנושא + שלד האתר |
| `questions-*.js` | מאגרי השאלות הפתוחות לפי שנה |
| `questions-mc*.js` | השאלות האמריקאיות (‏92) |
| `topic-d0*.js` | פריימים לנגני האנימציה בעמודי הנושא |
| `walkthrough-<שנה>.js` | פתרונות מונפשים לפי שנה |
| `exam-archive.js` | קטלוג 21 המבחנים ו-84 שאלות הארכיון |
| `labs.js` | חמש המעבדות החופשיות |
| `bank-walk-<שנה>.js` | פתרונות מונפשים לשאלות המאגר |
| `formulas.html` | דף הנוסחאות להדפסה |
| `assets/exams/` | 32 קובצי PDF קנוניים — עובד גם offline |

**מנועים — לא לערוך:**
`viz.js` (הנפשה) · `adaptive-quiz.js` (מאמן) · `exam-sim.js` (סימולטור) · `walkthrough-ui.js` (תצוגת פתרונות)

**תיעוד:**
`EXAM-MAP.md` (מפת המבחן) · `SOURCE-MAP.md` (מקורות) · `STATUS.md` (מצב הפרויקט) · `AGENT-SPEC-*.md` (מפרטים לסוכנים)

## עדכון ודחיפה

```bash
./bump-version.sh && git add -A && git commit -m "תיאור" && git push
```

`bump-version.sh` מוסיף `?v=<commit>` לכל קובצי ה-JS/CSS — בלעדיו דפדפנים
יגישו גרסה ישנה מהמטמון.

## המקורות

חומר הקורס המקורי (שאלונים, פתרונות, גיליונות, דפי נוסחאות):
`~/Library/Mobile Documents/com~apple~CloudDocs/הנדסת חשמל/שנה 2 - סמס ב’/גלים`

ההתקדמות של כל משתמש נשמרת מקומית בדפדפן תחת הקידומת `galim90929-`.
