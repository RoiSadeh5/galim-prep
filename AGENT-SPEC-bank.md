# מפרט לסוכני מאגר השאלות — גלים 90929

## הפלט
קובץ אחד: `/Users/roisadeh/Desktop/Projects/galim-prep/questions-<שם>.js` בתבנית:

```js
window.BANK_OPEN_QUESTIONS = (window.BANK_OPEN_QUESTIONS || []).concat([
  {
    id: "EX-2026A-Q1",              /* ייחודי: EX-<שנה><מועד>-Q<מס> */
    cat: "מיתר-מונע-בקצה",          /* ראה רשימת קטגוריות למטה */
    difficulty: 3,                   /* 1–4, לפי הקושי בפועל */
    skills: ["שחזור-ממהירות", "תחומי-תוקף"],   /* 1–3 תגיות חופשיות קצרות */
    origin: "recent",                /* recent = 2024–2026 · official = 2021–2023 ומבחני הדוגמה */
    kind: "open",                    /* חובה */
    qtype: "analysis",
    sourceLabel: "🎓 מבחן 2026 מועד א' · שאלה 1",
    prompt: "<b>נוסח מלא ועומד בפני עצמו</b> — כל הנתונים, כל הסעיפים (א)–(ד) עם הניקוד.",
    hint: "רמז אחד שמכוון לשיטה בלי לפתור.",
    html: '<details class="sol"><summary>פתרון מלא</summary><div class="body">' +
          '<p><b>(א)</b> …פתרון סעיף-סעיף…</p>' +
          '</div></details>'
  }
]);
```

`node --check` חייב לעבור על הקובץ.

## קטגוריות (חייבות להתאים בדיוק ל-config.js)
`מיתר-מונע-בקצה` · `דלמבר` · `אנרגיה-והספק` · `צומת-אימפדנסים` ·
`גלים-עומדים-פורייה` · `קו-תמסורת` · `משוואת-הגלים` · `גלים-הרמוניים`

שאלת מבחן שלמה מקבלת את הקטגוריה של הנושא הראשי שלה (לא של סעיף האנרגיה שבתוכה).

## כללי תוכן
1. **פריט = שאלת מבחן שלמה** (4 סעיפים, 25 נק') — לא לפצל לסעיפים.
2. **prompt** חייב לכלול את כל הנתונים המספריים, הפונקציות (ב-KaTeX), והסעיפים עם הניקוד. מי שקורא רק את ה-prompt יכול לפתור.
3. **פתרון** ב-html בתוך `<details class="sol">`: סעיף-סעיף, עם הדרך (לא רק תשובה), נוסחאות ב-KaTeX, תשובות סופיות מודגשות עם יחידות. **לאמת כל חישוב בעצמך מול הפתרון הרשמי** — אם מצאת סתירה, פתור נכון וציין `<!-- הערה: -->` בקובץ.
4. **מתמטיקה ב-JS strings**: לוכסן כפול! `"\\(v=\\sqrt{T/\\rho}\\)"`, `"$$…$$"`. אין Unicode subscripts בתוך נוסחאות.
5. שרטוטים בשאלה (פולס משולש וכו') — לתאר במילים מדויקות בתוך ה-prompt (רוחב, גובה, כיוון), כי אין תמונות.
6. שאלות שאינן בליבה (גלי קול בגז, גלים א"מ, חבל תלוי/מסתובב) — **לדלג** ולציין בדיווח הסופי.
7. עברית מלאה, RTL. סימונים כמו בפתרונות הרשמיים (u=x−vt, R, τ).

## שמירה הדרגתית
לשמור את הקובץ אחרי כל 3–4 שאלות (Write מלא מחדש) — שלא יאבד כלום אם התהליך נקטע.
